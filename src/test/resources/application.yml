spring:
  ai:
    openai:
      api-key: test-api-key
      base-url: ${OPENAI_TEST_BASE_URL:http://localhost:9999}
      chat:
        options:
          model: gpt-4o-mini
      embedding:
        options:
          model: text-embedding-3-small
